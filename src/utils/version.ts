// Visible build counter shown in the plugin UI (currently the preview card).
// Bumped by 1 on each functional update so the user and I can align on progress.
// This is intentionally a simple integer counter (e.g. "UI v21"), not the
// package.json semantic version.
export const UI_VERSION_LABEL = 'UI v70';
